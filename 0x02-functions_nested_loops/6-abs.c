#include "main.h"

/**
 * _abs - computes the absolute vale of an integer
 * @t: The integer to be computed
 * Return: the absolue value
 */
int _abs(int t)
{
if (t >= 0)
return (t);
else
return (-t);
}
